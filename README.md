# dealornodealkotlin

Made w/ a TUI in Kotlin. Made w/ Maven. Run build.ps1 to build/execute

## Project Structure
./dealornodealkotlin/src -> contains all source code

## Dependencies
Java, Kotlin, Maven must be installed

## Scripts
build.ps1 -> builds with Maven to .jar and executes